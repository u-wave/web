import cx from 'classnames';
import React from 'react';

const Eta = ({ className }) => {
  return (
    <span className={cx('Eta', className)}>
      a while
    </span>
  );
};

export default Eta;
