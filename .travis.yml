language: node_js
sudo: false
node_js:
  - stable
  - 8.9

before_install:
  - npm install -g greenkeeper-lockfile@1
before_script:
  - greenkeeper-lockfile-update
  - npm run build

script:
  - npm test
  # Don't fail the build bc of this yet.
  # TODO enable again when istanbul updates to babel@7
  # - npm run test:check-coverage || true
  - npm run prod

after_script:
  - greenkeeper-lockfile-upload
